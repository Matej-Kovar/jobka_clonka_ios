# Run tests

```bash
cd /Users/danielvazac/Repos/Clonka/clonka-swift/ClonkaApp && xcodebuild test -project ClonkaApp.xcodeproj -scheme ClonkaApp -sdk iphonesimulator -destination 'platform=iOS Simulator,name=iPhone 16 Pro' -quiet
```

Report test results — pass/fail count and any failure details.
