# Build the project

Run in parallel:

```bash
cd /Users/danielvazac/Repos/Clonka/clonka-swift/ClonkaApp && xcodebuild build -project ClonkaApp.xcodeproj -scheme ClonkaApp -sdk iphonesimulator -destination 'generic/platform=iOS Simulator' -quiet
```

Report success or failure with any compiler errors.
